export default function LeadRouterPage() {
  return (
    <main className="min-h-screen px-6 py-16">
      <div className="mx-auto max-w-4xl">
        <h1 className="text-4xl font-semibold">Lead Router</h1>
        <p className="mt-3 text-zinc-300">Route inbound leads with rules + enrichment + approvals in one flow.</p>
      </div>
    </main>
  );
}
